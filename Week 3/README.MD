#Hand Gesture Recognition

Use MediaPipe or OpenCV to detect hand
landmarks in real time.
Classify simple gestures (e.g., thumbs up,
fist, open palm).
Map gestures to actions (e.g., play/pause,
increase volume) as a demo.
Export a demo script that runs on webcam
input.
